---
'sort-configs': patch
'sort-configs-typescript': patch
---

Ship the MIT `LICENSE` file in the published tarball. Both packages declared `license: MIT` but carried no license text, because the repository had no `LICENSE` file at all.
